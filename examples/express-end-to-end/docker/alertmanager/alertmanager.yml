route:
  receiver: junando
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1h

receivers:
  - name: junando
    webhook_configs:
      - url: 'http://junando-webhook:4000/webhook/alert'
        send_resolved: true
