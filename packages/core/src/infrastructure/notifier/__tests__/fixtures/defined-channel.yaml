channels:
  slack-sre:
    type: slack
    channel: '#sre-incidents'

pre-llm:
  rules:
    - id: route-to-sre
      condition:
        serviceName: payments-api
      actions:
        - type: route
          channel: slack-sre

post-llm:
  rules: []
