channels:
  teams-security:
    type: teams
    webhookUrlEnv: TEST_TEAMS_SECURITY_WEBHOOK_URL

pre-llm:
  rules:
    - id: route-to-teams-security
      condition:
        serviceName: payments-api
      actions:
        - type: route
          channel: teams-security

post-llm:
  rules: []
