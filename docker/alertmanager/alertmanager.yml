global:
  resolve_timeout: 5m
route:
  group_by: ['alertname', 'service']
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h
  receiver: junando
receivers:
  - name: junando
    webhook_configs:
      - url: 'http://host.docker.internal:4000/webhook/alert'
        send_resolved: true
